import { Container } from "react-bootstrap";

export default function LandingPage() {
  return (
    <Container className="page-shell">
      <section className="wip-body" aria-label="WorkTime">
        <h1>WIP</h1>
      </section>
    </Container>
  );
}
